spring:
  application:
    name: ms-claim-c
  profiles:
    active: dev

#  mybatis配置
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
